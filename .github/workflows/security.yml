name: Security

on:
  push:
    branches:
      - main
  pull_request:
    branches: [ "*" ]
  # Dependencies go stale between commits, so re-check on a schedule: a new
  # advisory against an unchanged dependency would otherwise go unnoticed.
  # This package has no dependencies, so in practice this watches the standard
  # library, which is where its entire attack surface lives.
  schedule:
    - cron: '0 6 * * 1'

permissions:
  contents: read

jobs:
  govulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: '1.27'

      # Reports only vulnerabilities that are actually reachable from this
      # code, so the signal stays high enough to act on.
      - name: Run govulncheck
        run: |
          go run golang.org/x/vuln/cmd/govulncheck@latest ./...
